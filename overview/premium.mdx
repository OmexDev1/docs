---
title: "Donator Perks"
description: "Gain access to exclusive rewards by donating to Omex."
icon: "dollar-sign"
---

## How to get Donator Perks

<Warning>
  Click [here](https://discord.gg/thbuzS7BxK) to purchase premium permanently, or vote [here](https://top.gg/bot/1413649582340182158) to access premium commands for free!
</Warning>

<Tabs>
  <Tab title="Tier 3">
    <AccordionGroup>
      <Accordion title="Last.fm Account Linking">
        Link your Last.fm account to Omex.

        <CodeGroup>

        ```text Syntax
        ,lastfm set (username)
        ```

        
        ```text Example
        ,lastfm set Aj.wtf
        ```

        </CodeGroup>
      </Accordion>
      <Accordion title="Custom Self Prefix">
        Set your own personal command prefix across all servers.

        <CodeGroup>

        ```text Syntax
        ,prefix self set (prefix)
        ```

        
        ```text Example
        ,prefix self set ??
        ```

        </CodeGroup>
      </Accordion>
    </AccordionGroup>
  </Tab>
  <Tab title="Tier 2">
    <AccordionGroup>
      <Accordion title="ChatGPT">
        Ask ChatGPT a question directly from Discord.

        <CodeGroup>

        ```text Syntax
        ,chatgpt (question)
        ```

        
        ```text Example
        ,chatgpt who are you
        ```

        </CodeGroup>
      </Accordion>
    </AccordionGroup>
  </Tab>
  <Tab title="Tier 1">
    <AccordionGroup>
      <Accordion title="Statistics Module Access">
        Unlock access to the **Statistics** module, allowing you to create live counter channels for your server.

        <CodeGroup>

        ```text Example
        ,statistics set members #members
        ```

        </CodeGroup>
      </Accordion>
      <Accordion title="emoji steal">
        Steal the provided emoji

        <CodeGroup>

        ```text Example
        steal (emoji) (name)
        ```

        </CodeGroup>
      </Accordion>
    </AccordionGroup>
  </Tab>
</Tabs>